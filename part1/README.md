# Part 1: Zipf's Law and Corpus Analysis

## Setup
```bash
pip install -r requirements.txt
```

## Running
```bash
python part1_zipf.py
```
This script downloads the Brown corpus from NLTK, runs Zipf's law analyses, prints answers to Q1-Q5 to the console, and saves the plots into the `output/` directory.
